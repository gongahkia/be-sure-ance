[build]
  command = "python3 -m pip install -r requirements.txt && python3 -m playwright install chromium && npm --prefix src/be-sure-ance-app ci && python3 -m src.lib.static_app_data --run-scrapers --fallback-demo --output src/be-sure-ance-app/public/data/app-data.json && npm --prefix src/be-sure-ance-app run build:app"
  publish = "src/be-sure-ance-app/dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = "default-src 'self'; connect-src 'self' https://*.sentry.io https://*.ingest.sentry.io; img-src 'self' data: https:; script-src 'self'; style-src 'self' 'unsafe-inline'; font-src 'self' data:; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'; upgrade-insecure-requests"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer"
    Permissions-Policy = "camera=(), microphone=(), geolocation=(), payment=(), usb=(), interest-cohort=()"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"
