[tool.black]
line-length = 100
target-version = ["py311"]

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [
  ".git",
  ".venv",
  "src/be-sure-ance-app/dist",
  "src/be-sure-ance-app/node_modules",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
